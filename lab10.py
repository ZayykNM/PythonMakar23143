"""
Лабораторная работа №10
Вариант 2

Задание:
Написать функцию, которая создает массивы из десяти нулей, единиц и пятерок.

Описание работы:
Используется библиотека NumPy для эффективной генерации массивов.
- zeros() создает массив нулей.
- ones() создает массив единиц.
- full() (или умножение) создает массив из произвольных чисел (пятерок).
"""

import numpy as np

def create_arrays():
    """
    Создает и выводит три массива:
    1. Массив из 10 нулей.
    2. Массив из 10 единиц.
    3. Массив из 10 пятерок.
    """
    print("--- Генерация массивов NumPy ---")

    # 1. Массив из 10 нулей
    # dtype=int указывает, что нам нужны целые числа (0), а не дробные (0.0)
    zeros_array = np.zeros(10, dtype=int)
    print(f"Массив нулей:   {zeros_array}")

    # 2. Массив из 10 единиц
    ones_array = np.ones(10, dtype=int)
    print(f"Массив единиц:  {ones_array}")

    # 3. Массив из 10 пятерок
    # Способ А: Создать единицы и умножить на 5 (векторизация)
    fives_array = np.ones(10, dtype=int) * 5
    
    # Способ Б (альтернативный): Использовать функцию full
    # fives_array = np.full(10, 5)
    
    print(f"Массив пятерок: {fives_array}")

    return zeros_array, ones_array, fives_array

# --- Блок запуска ---
if __name__ == "__main__":
    create_arrays()