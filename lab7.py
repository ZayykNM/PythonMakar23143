"""
Лабораторная работа №7
Вариант 6

Задание:
Написать класс Shape, который является родительским для класса Square.
Square содержит конструктор, принимающий длину. Оба класса содержат метод area().
У Shape площадь равна 0, у Square рассчитывается математически.

Описание работы:
Демонстрация принципов наследования и полиморфизма в Python.
Класс Square наследует методы класса Shape, но переопределяет метод area()
для своей логики.
"""

class Shape:
    """
    Базовый класс 'Фигура'.
    """
    def area(self):
        """
        Возвращает площадь фигуры.
        Для абстрактной фигуры площадь по умолчанию равна 0.
        """
        return 0


class Square(Shape):
    """
    Класс 'Квадрат', наследник класса Shape.
    """
    def __init__(self, length):
        """
        Конструктор класса.
        :param length: Длина стороны квадрата
        """
        self.length = length

    def area(self):
        """
        Переопределенный метод расчета площади.
        Формула: длина * длина
        """
        return self.length * self.length


# --- Блок тестирования ---
if __name__ == "__main__":
    # 1. Создаем объект базового класса
    basic_shape = Shape()
    print(f"Площадь абстрактной фигуры (Shape): {basic_shape.area()}")

    # 2. Создаем объект квадрата со стороной 5
    side = 5
    my_square = Square(side)
    print(f"Площадь квадрата со стороной {side}: {my_square.area()}")

    # 3. Создаем объект квадрата со стороной 10
    side2 = 10
    big_square = Square(side2)
    print(f"Площадь квадрата со стороной {side2}: {big_square.area()}")